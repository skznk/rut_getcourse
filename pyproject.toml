[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "cell"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
  "selenium>=4.36",
]

[project.scripts]
finalshot = "cell.__main__:main"
